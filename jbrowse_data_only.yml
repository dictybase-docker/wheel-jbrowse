jbdata:
    image: progrium/busybox 
    volumes: 
        - /data
jblog:
    image: progrium/busybox 
    volumes: 
        - /log
jbconfig:
    image: progrium/busybox
    volumes:
        - config:/config
jbhub:
    image: progrium/busybox
    volumes_from:
        - jbdata
        - jbconfig
        - jblog
jbrestore:
    build: spoke-jbrowse-tracks-conf/
    volumes:
        - data:/tmp/jbrowse
    volumes_from:
        - jbhub
    command: /bin/sh -c "tar xzf /tmp/jbrowse/jbrowse-data.tar.gz --strip-components=1 -C /data && app copy --nowait && app watchcopy"
jbfrontend:
    build: spoke-jbrowse-frontend/
    volumes_from:
        - jbhub
    command: app serve
    ports:
        - 9595:9595
    env_file:
        - ./env/timezone.env
